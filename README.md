# perfect

A web-based profile viewer for Linux `perf.data` files, modeled on Xcode's Instruments.app.

## Run

```sh
npm install
npm start
```

Then open http://localhost:5173 and drop a `perf.data` file on the page.
